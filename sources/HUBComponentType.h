#import <Foundation/Foundation.h>

/// Enum describing various Hub Framework component types
typedef NS_ENUM(NSInteger, HUBComponentType) {
    /// The type of component used for a view's header
    HUBComponentTypeHeader,
    /// The type of component used for a view's body
    HUBComponentTypeBody
};
