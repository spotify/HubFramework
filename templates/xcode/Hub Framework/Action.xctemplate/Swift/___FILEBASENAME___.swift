import Foundation
import HubFramework

class ___FILEBASENAMEASIDENTIFIER___: NSObject, HUBAction {
    func perform(with context: HUBActionContext) -> Bool {
        // Perform the action, and return a BOOL indicating whether it was performed or not
        return false
    }
}
