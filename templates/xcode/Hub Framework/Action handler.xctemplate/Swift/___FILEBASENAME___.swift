import Foundation
import HubFramework

class ___FILEBASENAMEASIDENTIFIER___: NSObject, HUBActionHandler {
    func handleAction(with context: HUBActionContext) -> Bool {
        // Optionally handle an action with a given context, and return whether it was handled or not
        return false
    }
}
